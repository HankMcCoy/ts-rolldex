# Rolldex

The agent guide for this repo lives in `AGENTS.md`, shared with every other
coding agent so there's one source of truth rather than a Claude-specific fork.
It is imported below — read it before writing code.

@AGENTS.md
